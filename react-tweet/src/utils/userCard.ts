import UserName from "./userName";
import UserAccount from "./userAccount";
import FollowButton from "./followButton";

export default interface UserCard extends UserName, UserAccount, FollowButton{
    
}